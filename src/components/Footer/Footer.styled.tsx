import styled from "styled-components";
import { bgColor, menuColor } from "../../App/App.styled";

export const FooterWrapper = styled.div`
  background-color: ${bgColor};
  color: ${menuColor};
  text-align: center;
  padding: 10px;
  position: relative;
  bottom: 0;
  width: 100%;
`;
